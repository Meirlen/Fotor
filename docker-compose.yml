version: '3.9'

services:
  face_swap_service:
    build: .
    container_name: face_swap_container
    environment:
      - PYTHONUNBUFFERED=1  # Чтобы логи выводились в реальном времени
    volumes:
      - .:/app  # Монтируем текущую директорию в контейнер
    restart: unless-stopped
